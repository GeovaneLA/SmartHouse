import 'package:flutter/material.dart';

class userDados {
  String nome = '';
  String email = '';

  var familias = [];

  userDados({
    required this.nome,
    required this.email,
    required this.familias,
  });
}
